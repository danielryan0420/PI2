{
  "name": "baseball-scorekeeper",
  "version": "0.1.0",
  "private": true,
  "description": "Live baseball scorekeeping app with real-time scoreboard overlay for streaming",
  "scripts": {
    "install-all": "cd client && npm install && cd ..",
    "dev": "npm run dev --workspace=client -- &",
    "build": "cd client && npm run build && cd ..",
    "start": "python3 server.py"
  }
}
